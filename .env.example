# This is the main .env file that defines which environment to use
# Valid options: development, staging, production

# Set the environment to load specific .env.{ENV} file
ENV=development

# You can override specific variables here, but it's recommended
# to maintain environment-specific configurations in .env.{ENV} files